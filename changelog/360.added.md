Add a strict run mode (`EXAMPLE_STRICT=1`) to the example tests — every example runs at full size (`FAST_TEST_MODE` stripped) to completion, and a timeout is a failure — plus a scheduled GPU workflow (`example_full_run.yml`) that runs it every two days. Examples for deprecated modules (`phe`, `hpo`) are skipped, and the slow `feature_selection`, `pdp_example`, and `pval_crt_demo` examples now shrink under `FAST_TEST_MODE=1` so the PR smoke gate verifies them to completion.
