Add a strict run mode (`EXAMPLE_STRICT=1`) to the example tests: every example runs at full size to completion, and a timeout is a failure. Examples for deprecated modules (`phe`, `hpo`) are skipped.
