Add a full run mode (`EXAMPLE_FULL=1`) to the example tests — every example runs at full size (`FAST_TEST_MODE` stripped) to completion, and a timeout is a failure — plus a scheduled GPU workflow (`test_examples_full.yml`) that runs it every two days. The slow `feature_selection`, `pdp_example`, and `pval_crt_demo` examples now shrink under `FAST_TEST_MODE=1` so the PR smoke gate verifies them to completion.
