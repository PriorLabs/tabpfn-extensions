`score_classification` now accepts `"auroc"` as an alias for `"roc"`, so callers can use either name interchangeably (e.g. scripts passing `"auroc"` no longer fall through to the wrong scoring path).
