The Bayesian optimization example no longer crashes on CUDA machines with a device mismatch: the botorch benchmark objective is moved to the evaluation device so its bounds buffer matches the candidate tensors.
