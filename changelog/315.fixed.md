Fix `get_tabpfn_models()` raising `NameError` at import time when the API/client backend is selected (`USE_TABPFN_LOCAL=false`) with `tabpfn-client` installed: the client wrapper classes are now bound to the names the backend selector reads, so API inference resolves correctly again.
