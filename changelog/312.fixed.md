Infer TabPFN's supported class count from the model (`utils.get_max_num_classes`) instead of hard-coding it to 10, so the unsupervised model and `ManyClassClassifier` work correctly with TabPFN-3's higher class limit.
