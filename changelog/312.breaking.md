Require `tabpfn>=8.0.0` (was `>=7.0.0`). The class-count limit is now read directly from the model's inference config, so the fallback that assumed a 10-class cap for older TabPFN builds has been removed.
