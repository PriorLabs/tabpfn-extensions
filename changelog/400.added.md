Add the `image` extension: `TabPFNWithImages` and `ImageTransformer` turn declared image columns, file paths, base64 strings, bytes or PIL images, into frozen DINOv3 features before TabPFN sees them.
