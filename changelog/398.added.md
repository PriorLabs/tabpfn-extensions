Add the `image` extension: `TabPFNWithImages` and `ImageTransformer` turn declared image columns, base64 strings or bytes, into frozen DINOv3 features before TabPFN sees them. Install the optional dependencies with `pip install "tabpfn-extensions[image]"`.
