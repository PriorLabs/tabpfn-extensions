Support an optional `dag` argument in `TabPFNUnsupervisedModel.impute()` and `generate_synthetic_data()` to condition synthesis/imputation on a known causal structure (features processed in topological order, each conditioned on its DAG parents).
